sudo: required

language: csharp
mono: none
solution: FinalProjectRegistration.sln

addons:
  snaps:
    - name: dotnet-sdk
    - channel: lts/stable
    - confinement: classic

before_script:
  sudo snap alias dotnet-sdk.dotnet dotnet


script:
  - dotnet restore
  - dotnet build
  - dotnet test
