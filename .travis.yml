sudo: required

language: csharp
mono: none
solution: FinalProjectRegistration.sln

addons:
  snaps:
    - name: dotnet-sdk
    - confinement: classic
    - channel: 3.1/stable

before_script:
  sudo snap alias dotnet-sdk.dotnet dotnet


script:
  - dotnet restore
  - dotnet build
  - dotnet test
